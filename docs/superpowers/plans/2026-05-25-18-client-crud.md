# Plan 18 — 顧問先 CRUD 管理

作成日: 2026-05-25

---

## T1: POST /api/clients — テスト & 実装

**Files:**
- `server/src/services/client-service.ts` (追記)
- `server/src/routes/clients.ts` (追記)
- `server/tests/routes/clients.test.ts` (追記)

**Steps:**
1. テスト追加: POST 201 (minimal fields) / POST 400 missing name / POST 400 end < start → **fail 確認**
2. `client-service.ts` に `createClient(data, firmId)` を追加
3. `clients.ts` に `app.post('/api/clients', ...)` を追加 (Zod バリデーション)
4. テスト **pass 確認**

**Commit:** `feat(spec 18): POST /api/clients — create client`

---

## T2: PATCH /api/clients/:id — テスト & 実装

**Files:**
- `server/src/services/client-service.ts` (追記)
- `server/src/routes/clients.ts` (追記)
- `server/tests/routes/clients.test.ts` (追記)

**Steps:**
1. テスト追加: PATCH 200 / PATCH 404 unknown / PATCH 404 other-tenant → **fail 確認**
2. `client-service.ts` に `updateClient(id, firmId, data)` を追加
3. `clients.ts` に `app.patch('/api/clients/:id', ...)` を追加
4. テスト **pass 確認**

**Commit:** `feat(spec 18): PATCH /api/clients/:id — update client`

---

## T3: DELETE /api/clients/:id — テスト & 実装

**Files:**
- `server/src/services/client-service.ts` (追記)
- `server/src/routes/clients.ts` (追記)
- `server/tests/routes/clients.test.ts` (追記)

**Steps:**
1. テスト追加: DELETE 200 / DELETE 404 unknown / DELETE 404 other-tenant → **fail 確認**
2. `client-service.ts` に `deleteClient(id, firmId)` を追加
3. `clients.ts` に `app.delete('/api/clients/:id', ...)` を追加
4. テスト **pass 確認**

**Commit:** `feat(spec 18): DELETE /api/clients/:id — delete client`

---

## T4: フロント — 設定ビューに顧問先管理セクション追加

**Files:**
- `script.js`

**Steps:**
1. `renderSettings()` に顧問先管理 HTML セクションを追加
2. `loadClientList()` async function でAPIから一覧取得→描画
3. action handler: `settings-add-client` / `settings-save-client` / `settings-edit-client` / `settings-delete-client`
4. `node --check script.js` で構文確認

**Commit:** `feat(spec 18): 設定ビューに顧問先管理UI追加`

---

## T5: 全テスト pass 確認 & クリーンアップ

**Steps:**
1. `npm test` → 全テスト pass
2. `npx tsc --noEmit` → 既知エラー以外なし
3. MEMORY.md 更新

**Commit:** なし（確認のみ）
