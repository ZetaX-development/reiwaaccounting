# Spec 11: 証憑 OCR (OpenAI Vision で項目抽出) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** spec 10 で保存済みの Voucher 画像に対し、OpenAI Vision を呼んで 5 フィールド（issue_date / vendor_name / addressee / amount / invoice_number）を抽出して `Voucher.ocrJson` に保存する。POST /api/vouchers 直後に fire-and-forget で自動実行し、UI でステータス+結果を見せる。

**Architecture:** `ocr-service.extractVoucherFields(buffer, mimeType)` が OpenAI SDK を呼ぶ純関数寄りサービス。`voucher-service.runOcrForVoucher(id)` がランナー（status を回しつつサービスを呼ぶ）。POST ハンドラ末尾で `setImmediate` 起動。OPENAI_API_KEY 未設定なら何もしない（既存テスト互換）。

**Tech Stack:** openai 4.x SDK / Prisma migration / Vitest (`vi.spyOn` で OpenAI を mock) / Vanilla JS フロント。

**Spec source:** `docs/superpowers/specs/2026-05-18-11-voucher-ocr-design.md`

---

## File Structure

| File | Action | Responsibility |
|---|---|---|
| `server/package.json` | Modify | `openai` 依存追加 |
| `server/src/env.ts` | Modify | `OPENAI_API_KEY?` + `OPENAI_VISION_MODEL=gpt-5` |
| `server/.env.example` | Modify | OPENAI セクション追加 |
| `server/prisma/schema.prisma` | Modify | `Voucher.ocrError String?` |
| `server/prisma/migrations/<ts>_add_ocr_error/` | Create | Prisma 自動生成 |
| `server/src/services/ocr-service.ts` | Create | `extractVoucherFields` |
| `server/src/services/voucher-service.ts` | Modify | `runOcrForVoucher` を追加、`listVouchers` の戻りに ocrJson/ocrError/ocrAt を含める |
| `server/src/routes/vouchers.ts` | Modify | POST 末尾で setImmediate kick、新規 POST /:id/ocr ルート |
| `server/tests/services/ocr-service.test.ts` | Create | OpenAI SDK spy で 3 ケース |
| `server/tests/services/voucher-service.test.ts` | Modify | `runOcrForVoucher` のテスト追加 |
| `server/tests/routes/vouchers.test.ts` | Modify | POST /:id/ocr のテスト追加、GET 戻りに新規フィールドが含まれることを確認 |
| `script.js` | Modify | カードに OCR ステータス + 結果表示、再試行ボタン、ポーリング |
| `styles.css` | Modify | OCR ステータス表示用のクラス |

---

## Task 1: openai SDK 依存追加 + env 拡張

**Files:** `server/package.json`, `server/src/env.ts`, `server/.env.example`

- [ ] **Step 1: 依存追加**

```bash
cd /home/kkouta/poc/zeimee/server
npm install openai@^4.78.0
```

- [ ] **Step 2: env.ts に 2 行追加**

`server/src/env.ts` の `LINEWORKS_PRIVATE_KEY` の次に:

```ts
  OPENAI_API_KEY: z.string().default(''),
  OPENAI_VISION_MODEL: z.string().default('gpt-5'),
```

- [ ] **Step 3: .env.example に追記**

末尾に:

```
# OpenAI Vision OCR (spec 11)
# Required for voucher OCR. Without this, uploaded vouchers stay in ocrStatus=pending.
OPENAI_API_KEY=
# Default is gpt-5 (highest accuracy). Override to gpt-4o or gpt-4o-mini to lower cost.
OPENAI_VISION_MODEL=gpt-5
```

- [ ] **Step 4: 確認**

```bash
cd /home/kkouta/poc/zeimee/server
npm run build 2>&1 | head -5    # tsc が通るか
```

(spec 10 と同様、既存の TS overload エラーは出る。新規エラーが無ければ OK)

- [ ] **Step 5: Commit**

```bash
cd /home/kkouta/poc/zeimee
git add server/package.json server/package-lock.json server/src/env.ts server/.env.example
git commit -m "chore(spec 11): add openai dep and OPENAI_* env vars"
```

---

## Task 2: Prisma — Voucher.ocrError 追加

**Files:** `server/prisma/schema.prisma`, `server/prisma/migrations/...`

- [ ] **Step 1: schema.prisma に追加**

`Voucher` モデルの `matchStatus` の上に:

```prisma
  ocrError       String?
```

- [ ] **Step 2: マイグレーション**

```bash
cd /home/kkouta/poc/zeimee/server
npm run prisma:migrate -- --name add-voucher-ocr-error
```

- [ ] **Step 3: テーブル確認**

```bash
docker compose exec postgres psql -U zeimee -d zeimee -c '\d "Voucher"' | grep ocrError
```

Expected: `ocrError | text` の行が出る。

- [ ] **Step 4: Commit**

```bash
cd /home/kkouta/poc/zeimee
git add server/prisma/schema.prisma server/prisma/migrations/
git commit -m "feat(spec 11): add Voucher.ocrError field"
```

---

## Task 3: ocr-service.ts (TDD, 3 ケース)

**Files:** `server/src/services/ocr-service.ts`, `server/tests/services/ocr-service.test.ts`

- [ ] **Step 1: 失敗テストを書く**

Create `server/tests/services/ocr-service.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { extractVoucherFields } from '../../src/services/ocr-service.js';
import OpenAI from 'openai';

vi.mock('openai');

beforeEach(() => {
  vi.clearAllMocks();
  process.env.OPENAI_API_KEY = 'sk-test';
  process.env.OPENAI_VISION_MODEL = 'gpt-5';
});

describe('extractVoucherFields', () => {
  it('parses a valid OpenAI response into ExtractedFields', async () => {
    const create = vi.fn().mockResolvedValue({
      choices: [
        {
          message: {
            content: JSON.stringify({
              issue_date: '2026-05-15',
              vendor_name: '青山デザイン',
              addressee: '株式会社サンプル',
              amount: 3200,
              invoice_number: 'T1234567890123',
            }),
          },
        },
      ],
    });
    (OpenAI as unknown as vi.Mock).mockImplementation(() => ({
      chat: { completions: { create } },
    }));

    const result = await extractVoucherFields(
      Buffer.from([0xff, 0xd8, 0xff, 0xe0]),
      'image/jpeg',
    );
    expect(result.issue_date).toBe('2026-05-15');
    expect(result.vendor_name).toBe('青山デザイン');
    expect(result.addressee).toBe('株式会社サンプル');
    expect(result.amount).toBe(3200);
    expect(result.invoice_number).toBe('T1234567890123');

    // Verify the image was passed as data URL
    expect(create).toHaveBeenCalledOnce();
    const call = create.mock.calls[0][0];
    expect(call.model).toBe('gpt-5');
    const imagePart = call.messages
      .flatMap((m: { content: unknown }) =>
        Array.isArray(m.content) ? m.content : [],
      )
      .find((p: { type: string }) => p.type === 'image_url');
    expect(imagePart.image_url.url).toMatch(/^data:image\/jpeg;base64,/);
  });

  it('returns nulls for unreadable fields', async () => {
    const create = vi.fn().mockResolvedValue({
      choices: [
        {
          message: {
            content: JSON.stringify({
              issue_date: null,
              vendor_name: null,
              addressee: null,
              amount: null,
              invoice_number: null,
            }),
          },
        },
      ],
    });
    (OpenAI as unknown as vi.Mock).mockImplementation(() => ({
      chat: { completions: { create } },
    }));

    const result = await extractVoucherFields(
      Buffer.from([0x89, 0x50]),
      'image/png',
    );
    expect(result).toEqual({
      issue_date: null,
      vendor_name: null,
      addressee: null,
      amount: null,
      invoice_number: null,
    });
  });

  it('throws when OpenAI returns malformed JSON', async () => {
    const create = vi.fn().mockResolvedValue({
      choices: [{ message: { content: 'not json' } }],
    });
    (OpenAI as unknown as vi.Mock).mockImplementation(() => ({
      chat: { completions: { create } },
    }));

    await expect(
      extractVoucherFields(Buffer.from([0xff]), 'image/jpeg'),
    ).rejects.toThrow();
  });
});
```

- [ ] **Step 2: 失敗確認**

```bash
cd /home/kkouta/poc/zeimee/server
npx vitest run tests/services/ocr-service.test.ts
```

Expected: モジュール解決エラー or 3 ケース fail。

- [ ] **Step 3: 実装**

Create `server/src/services/ocr-service.ts`:

```ts
import OpenAI from 'openai';
import { z } from 'zod';
import { env } from '../env.js';

const ExtractedFieldsSchema = z.object({
  issue_date: z.string().nullable(),
  vendor_name: z.string().nullable(),
  addressee: z.string().nullable(),
  amount: z.number().nullable(),
  invoice_number: z.string().nullable(),
});

export type ExtractedFields = z.infer<typeof ExtractedFieldsSchema>;

const SYSTEM_PROMPT = `あなたは日本の領収書・請求書から構造化データを抽出するアシスタントです。
画像から以下の 5 フィールドを抽出して JSON で返してください。
- issue_date: 発行日 (YYYY-MM-DD 形式、和暦は西暦に変換)
- vendor_name: 発行者の名称（領収書を切った側）
- addressee: 宛名（領収書の「上様」など曖昧な場合も含めそのまま）
- amount: 税込合計金額（整数の円、カンマ・¥記号は除く）
- invoice_number: インボイス登録番号（T で始まる 13 桁）

読み取れない / 該当が無いフィールドは null にしてください。推測で埋めないこと。`;

const JSON_SCHEMA = {
  name: 'voucher_extraction',
  strict: true,
  schema: {
    type: 'object',
    additionalProperties: false,
    properties: {
      issue_date: { type: ['string', 'null'] },
      vendor_name: { type: ['string', 'null'] },
      addressee: { type: ['string', 'null'] },
      amount: { type: ['number', 'null'] },
      invoice_number: { type: ['string', 'null'] },
    },
    required: [
      'issue_date',
      'vendor_name',
      'addressee',
      'amount',
      'invoice_number',
    ],
  },
};

export async function extractVoucherFields(
  imageData: Buffer,
  mimeType: string,
): Promise<ExtractedFields> {
  const client = new OpenAI({ apiKey: env.OPENAI_API_KEY });
  const dataUrl = `data:${mimeType};base64,${imageData.toString('base64')}`;
  const completion = await client.chat.completions.create({
    model: env.OPENAI_VISION_MODEL,
    messages: [
      { role: 'system', content: SYSTEM_PROMPT },
      {
        role: 'user',
        content: [
          { type: 'text', text: 'この画像から指定 5 項目を抽出してください。' },
          { type: 'image_url', image_url: { url: dataUrl } },
        ],
      },
    ],
    response_format: { type: 'json_schema', json_schema: JSON_SCHEMA },
  });
  const text = completion.choices[0]?.message?.content;
  if (!text) throw new Error('OpenAI returned empty content');
  const parsed = JSON.parse(text);
  return ExtractedFieldsSchema.parse(parsed);
}
```

- [ ] **Step 4: パス確認**

```bash
cd /home/kkouta/poc/zeimee/server
npx vitest run tests/services/ocr-service.test.ts
```

Expected: 3/3 PASS。

- [ ] **Step 5: Commit**

```bash
cd /home/kkouta/poc/zeimee
git add server/src/services/ocr-service.ts server/tests/services/ocr-service.test.ts
git commit -m "feat(spec 11): ocr-service.extractVoucherFields with OpenAI Vision"
```

---

## Task 4: voucher-service.runOcrForVoucher + listVouchers 拡張 (TDD)

**Files:** `server/src/services/voucher-service.ts`, `server/tests/services/voucher-service.test.ts`

- [ ] **Step 1: 失敗テストを追加**

`server/tests/services/voucher-service.test.ts` の末尾に追加:

```ts
import * as ocrService from '../../src/services/ocr-service.js';

describe('runOcrForVoucher', () => {
  it('sets ocrStatus=done and saves extracted JSON', async () => {
    const meta = await createVoucher({
      clientId: null,
      filename: 'a.jpg',
      mimeType: 'image/jpeg',
      buffer: Buffer.from([0xff, 0xd8]),
      uploadedBy: null,
    });
    const spy = vi.spyOn(ocrService, 'extractVoucherFields').mockResolvedValue({
      issue_date: '2026-05-15',
      vendor_name: '青山デザイン',
      addressee: null,
      amount: 3200,
      invoice_number: null,
    });
    await runOcrForVoucher(meta.id);
    const row = await prisma.voucher.findUnique({ where: { id: meta.id } });
    expect(row?.ocrStatus).toBe('done');
    expect(row?.ocrJson).toEqual({
      issue_date: '2026-05-15',
      vendor_name: '青山デザイン',
      addressee: null,
      amount: 3200,
      invoice_number: null,
    });
    expect(row?.ocrError).toBeNull();
    expect(spy).toHaveBeenCalledOnce();
  });

  it('sets ocrStatus=failed and saves ocrError on extraction error', async () => {
    const meta = await createVoucher({
      clientId: null,
      filename: 'b.jpg',
      mimeType: 'image/jpeg',
      buffer: Buffer.from([0xff, 0xd8]),
      uploadedBy: null,
    });
    vi.spyOn(ocrService, 'extractVoucherFields').mockRejectedValue(
      new Error('OpenAI 503'),
    );
    await runOcrForVoucher(meta.id);
    const row = await prisma.voucher.findUnique({ where: { id: meta.id } });
    expect(row?.ocrStatus).toBe('failed');
    expect(row?.ocrError).toBe('OpenAI 503');
  });

  it('is a no-op when voucher does not exist', async () => {
    await expect(runOcrForVoucher('does-not-exist')).resolves.toBeUndefined();
  });
});

describe('listVouchers ocr fields', () => {
  it('returns ocrJson, ocrError, ocrAt in the meta', async () => {
    const meta = await createVoucher({
      clientId: null,
      filename: 'c.jpg',
      mimeType: 'image/jpeg',
      buffer: Buffer.from([0xff]),
      uploadedBy: null,
    });
    vi.spyOn(ocrService, 'extractVoucherFields').mockResolvedValue({
      issue_date: '2026-05-15',
      vendor_name: 'X',
      addressee: null,
      amount: 100,
      invoice_number: null,
    });
    await runOcrForVoucher(meta.id);
    const rows = await listVouchers({ clientId: null });
    const row = rows.find((r) => r.id === meta.id);
    expect(row?.ocrJson).toBeTruthy();
    expect(row?.ocrAt).toBeInstanceOf(Date);
  });
});
```

`runOcrForVoucher` の import を冒頭の import に追加し、`vi` を vitest import に足す:

```ts
import { describe, it, expect, beforeEach, afterAll, vi } from 'vitest';
import {
  createVoucher,
  listVouchers,
  getVoucherImage,
  deleteVoucher,
  runOcrForVoucher,
} from '../../src/services/voucher-service.js';
```

- [ ] **Step 2: 失敗確認**

```bash
cd /home/kkouta/poc/zeimee/server
npx vitest run tests/services/voucher-service.test.ts -t "runOcrForVoucher"
```

Expected: 3 ケース全部 FAIL (`runOcrForVoucher is not a function` 等)。

- [ ] **Step 3: voucher-service.ts に追加**

`server/src/services/voucher-service.ts` を編集:

a. `VoucherMeta` に 3 フィールドを追加:

```ts
export interface VoucherMeta {
  id: string;
  clientId: string | null;
  filename: string;
  mimeType: string;
  size: number;
  uploadedAt: Date;
  uploadedBy: string | null;
  ocrStatus: string;
  ocrJson: unknown;        // 追加
  ocrError: string | null; // 追加
  ocrAt: Date | null;      // 追加
  matchStatus: string;
}
```

b. `toMeta` も対応するフィールドを `select` から拾うように更新（既存の `select` リストに追加）。`Pick<Voucher, keyof VoucherMeta>` 型のままで OK（`unknown` は `JsonValue` の上位なので互換）。実際は `ocrJson: row.ocrJson as unknown` でキャスト。

c. ファイル末尾に追加:

```ts
import { extractVoucherFields } from './ocr-service.js';

export async function runOcrForVoucher(id: string): Promise<void> {
  const row = await prisma.voucher.findUnique({
    where: { id },
    select: { id: true, imageData: true, mimeType: true },
  });
  if (!row) return;
  await prisma.voucher.update({
    where: { id },
    data: { ocrStatus: 'processing' },
  });
  try {
    const result = await extractVoucherFields(
      Buffer.from(row.imageData),
      row.mimeType,
    );
    await prisma.voucher.update({
      where: { id },
      data: {
        ocrStatus: 'done',
        ocrJson: result,
        ocrAt: new Date(),
        ocrError: null,
      },
    });
  } catch (err: unknown) {
    const msg = err instanceof Error ? err.message : String(err);
    await prisma.voucher.update({
      where: { id },
      data: { ocrStatus: 'failed', ocrError: msg, ocrAt: new Date() },
    });
  }
}
```

d. `listVouchers` の `select` に `ocrJson: true, ocrError: true, ocrAt: true` を追加。

- [ ] **Step 4: パス確認**

```bash
cd /home/kkouta/poc/zeimee/server
npx vitest run tests/services/voucher-service.test.ts
```

Expected: 既存 9 + 新規 4 = 13 ケース PASS。

- [ ] **Step 5: Commit**

```bash
cd /home/kkouta/poc/zeimee
git add server/src/services/voucher-service.ts server/tests/services/voucher-service.test.ts
git commit -m "feat(spec 11): voucher-service runOcrForVoucher and OCR fields in meta"
```

---

## Task 5: POST /api/vouchers が setImmediate で OCR を起動 (TDD)

**Files:** `server/src/routes/vouchers.ts`, `server/tests/routes/vouchers.test.ts`

- [ ] **Step 1: テストを追加**

`server/tests/routes/vouchers.test.ts` の末尾に追加:

```ts
import * as voucherService from '../../src/services/voucher-service.js';

describe('POST /api/vouchers triggers OCR', () => {
  it('schedules runOcrForVoucher when OPENAI_API_KEY is set', async () => {
    process.env.OPENAI_API_KEY = 'sk-test';
    const spy = vi
      .spyOn(voucherService, 'runOcrForVoucher')
      .mockResolvedValue(undefined);
    const { payload, headers } = buildForm({
      file: Buffer.from([0xff, 0xd8, 0xff, 0xe0]),
      filename: 'IMG.jpg',
      contentType: 'image/jpeg',
    });
    const res = await app.inject({
      method: 'POST',
      url: '/api/vouchers',
      payload,
      headers,
    });
    expect(res.statusCode).toBe(201);
    // setImmediate fires on next tick; wait one microtask
    await new Promise((r) => setImmediate(r));
    expect(spy).toHaveBeenCalledOnce();
    expect(spy.mock.calls[0][0]).toBe(res.json().id);
  });

  it('does not schedule OCR when OPENAI_API_KEY is empty', async () => {
    process.env.OPENAI_API_KEY = '';
    const spy = vi
      .spyOn(voucherService, 'runOcrForVoucher')
      .mockResolvedValue(undefined);
    const { payload, headers } = buildForm({
      file: Buffer.from([0xff, 0xd8, 0xff, 0xe0]),
      filename: 'IMG.jpg',
      contentType: 'image/jpeg',
    });
    const res = await app.inject({
      method: 'POST',
      url: '/api/vouchers',
      payload,
      headers,
    });
    expect(res.statusCode).toBe(201);
    await new Promise((r) => setImmediate(r));
    expect(spy).not.toHaveBeenCalled();
  });
});
```

`vi` を import 群に追加:

```ts
import { describe, it, expect, beforeEach, afterAll, vi } from 'vitest';
```

- [ ] **Step 2: 失敗確認**

```bash
cd /home/kkouta/poc/zeimee/server
npx vitest run tests/routes/vouchers.test.ts -t "triggers OCR"
```

Expected: 2 ケース FAIL (spy が呼ばれない)。

- [ ] **Step 3: ルートを修正**

`server/src/routes/vouchers.ts` の POST ハンドラの `reply.code(201); return meta;` の直前に追加:

```ts
    if (process.env.OPENAI_API_KEY && process.env.OPENAI_API_KEY.length > 0) {
      setImmediate(() => {
        runOcrForVoucher(meta.id).catch(() => {
          // runOcrForVoucher itself swallows errors and records them in
          // ocrError; this catch is a defensive last resort.
        });
      });
    }
```

import を更新:

```ts
import {
  createVoucher,
  listVouchers,
  getVoucherImage,
  deleteVoucher,
  runOcrForVoucher,
} from '../services/voucher-service.js';
```

- [ ] **Step 4: パス確認**

```bash
cd /home/kkouta/poc/zeimee/server
npx vitest run tests/routes/vouchers.test.ts
```

Expected: 既存 8 + 新規 2 = 10 ケース PASS。

- [ ] **Step 5: Commit**

```bash
cd /home/kkouta/poc/zeimee
git add server/src/routes/vouchers.ts server/tests/routes/vouchers.test.ts
git commit -m "feat(spec 11): POST /api/vouchers triggers async OCR when OPENAI_API_KEY set"
```

---

## Task 6: POST /api/vouchers/:id/ocr (再試行ルート) (TDD)

**Files:** `server/src/routes/vouchers.ts`, `server/tests/routes/vouchers.test.ts`

- [ ] **Step 1: テストを追加**

`server/tests/routes/vouchers.test.ts` の末尾に追加:

```ts
describe('POST /api/vouchers/:id/ocr', () => {
  it('returns 202 and schedules runOcrForVoucher', async () => {
    const created = await prisma.voucher.create({
      data: {
        clientId: null,
        filename: 'r.jpg',
        mimeType: 'image/jpeg',
        size: 1,
        imageData: Buffer.from([0xff]),
      },
    });
    const spy = vi
      .spyOn(voucherService, 'runOcrForVoucher')
      .mockResolvedValue(undefined);
    const res = await app.inject({
      method: 'POST',
      url: `/api/vouchers/${created.id}/ocr`,
    });
    expect(res.statusCode).toBe(202);
    expect(res.json()).toEqual({ ok: true });
    await new Promise((r) => setImmediate(r));
    expect(spy).toHaveBeenCalledWith(created.id);
  });

  it('returns 404 for unknown id', async () => {
    const res = await app.inject({
      method: 'POST',
      url: '/api/vouchers/does-not-exist/ocr',
    });
    expect(res.statusCode).toBe(404);
    expect(res.json().error.code).toBe('NOT_FOUND');
  });
});
```

- [ ] **Step 2: 失敗確認**

```bash
cd /home/kkouta/poc/zeimee/server
npx vitest run tests/routes/vouchers.test.ts -t "POST /api/vouchers/:id/ocr"
```

Expected: 2 ケース FAIL。

- [ ] **Step 3: ルートを追加**

`server/src/routes/vouchers.ts` の `voucherRoutes` 関数内、DELETE の直後に追加:

```ts
  app.post<{ Params: { id: string } }>(
    '/api/vouchers/:id/ocr',
    async (req, reply) => {
      const row = await prisma.voucher.findUnique({
        where: { id: req.params.id },
        select: { id: true },
      });
      if (!row) {
        reply.code(404);
        return { error: { code: 'NOT_FOUND', message: 'voucher not found' } };
      }
      setImmediate(() => {
        runOcrForVoucher(req.params.id).catch(() => {});
      });
      reply.code(202);
      return { ok: true };
    },
  );
```

import を追加:

```ts
import { prisma } from '../lib/prisma.js';
```

- [ ] **Step 4: パス確認**

```bash
cd /home/kkouta/poc/zeimee/server
npx vitest run tests/routes/vouchers.test.ts
```

Expected: 12 ケース PASS。

- [ ] **Step 5: Commit**

```bash
cd /home/kkouta/poc/zeimee
git add server/src/routes/vouchers.ts server/tests/routes/vouchers.test.ts
git commit -m "feat(spec 11): POST /api/vouchers/:id/ocr retry route"
```

---

## Task 7: フロント — サムネカードに OCR ステータス + 抽出結果を表示

**Files:** `script.js`

- [ ] **Step 1: `renderVoucherRegister` の `cards` 生成箇所を更新**

`script.js` の `const cards = (appState.vouchers || []).map((v) => ...)` を以下に置換:

```js
  const cards = (appState.vouchers || [])
    .map((v) => {
      const ocr = v.ocrStatus || 'pending';
      let ocrHtml = '';
      if (ocr === 'pending' || ocr === 'processing') {
        ocrHtml = `<div class="voucher-ocr ocr-running"><span class="spinner-sm"></span>OCR 中…</div>`;
      } else if (ocr === 'failed') {
        ocrHtml = `<div class="voucher-ocr ocr-failed">OCR 失敗 <button class="voucher-ocr-retry" data-voucher-retry-ocr="${v.id}">再試行</button></div>`;
      } else if (ocr === 'done' && v.ocrJson) {
        const j = v.ocrJson;
        const amount = j.amount != null ? '¥' + j.amount.toLocaleString('ja-JP') : '—';
        const vendor = j.vendor_name ? escapeHtml(j.vendor_name) : '—';
        const date = j.issue_date ? escapeHtml(j.issue_date) : '—';
        ocrHtml = `
          <div class="voucher-ocr ocr-done">
            <div class="voucher-ocr-amount">${amount}</div>
            <div class="voucher-ocr-vendor">${vendor}</div>
            <div class="voucher-ocr-date">${date}</div>
          </div>`;
      }
      return `
      <div class="voucher-card" data-voucher-id="${v.id}">
        <img src="/api/vouchers/${v.id}/image" alt="${escapeHtml(v.filename)}" />
        <button class="voucher-delete" data-voucher-delete="${v.id}" aria-label="削除">×</button>
        <div class="voucher-meta">
          <div class="voucher-filename">${escapeHtml(v.filename)}</div>
          <div class="voucher-date">${new Date(v.uploadedAt).toLocaleString('ja-JP')}</div>
        </div>
        ${ocrHtml}
      </div>
    `;
    })
    .join('');
```

- [ ] **Step 2: 再試行ボタン用のイベント配線を追加**

`renderView` の vouchers-register ブロック内、`[data-voucher-delete]` の handler の直後に追加:

```js
    viewContent.querySelectorAll('[data-voucher-retry-ocr]').forEach((btn) => {
      btn.addEventListener('click', async (e) => {
        e.stopPropagation();
        const id = btn.dataset.voucherRetryOcr;
        try {
          const res = await fetch(`/api/vouchers/${id}/ocr`, { method: 'POST' });
          if (!res.ok) throw new Error('retry failed');
          appState.vouchersLoadedTab = null;
          await loadVouchers();
        } catch (err) {
          showToast(friendlyError(err));
        }
      });
    });
```

- [ ] **Step 3: 構文チェック + コミット**

```bash
cd /home/kkouta/poc/zeimee
node --check script.js
git add script.js
git commit -m "feat(spec 11): show OCR status and extracted fields on voucher cards"
```

---

## Task 8: フロント — OCR ポーリング (processing が残っている間 5 秒ごとに再ロード)

**Files:** `script.js`

- [ ] **Step 1: appState にタイマー参照を追加**

`appState` 内、`uploadQueue: [],` の直後に:

```js
  voucherPollTimer: null,
```

- [ ] **Step 2: `renderView` の vouchers-register ブロック末尾にポーリング起動**

`viewContent.querySelectorAll('[data-voucher-id]')` 配線の直後（モーダル close handler の前）に追加:

```js
    if (appState.voucherPollTimer) {
      clearInterval(appState.voucherPollTimer);
      appState.voucherPollTimer = null;
    }
    const hasPending = (appState.vouchers || []).some(
      (v) => v.ocrStatus === 'pending' || v.ocrStatus === 'processing',
    );
    if (hasPending) {
      appState.voucherPollTimer = setInterval(() => {
        if (appState.activeView !== 'vouchers-register') {
          clearInterval(appState.voucherPollTimer);
          appState.voucherPollTimer = null;
          return;
        }
        appState.vouchersLoadedTab = null;
        loadVouchers();
      }, 5000);
    }
```

- [ ] **Step 3: 構文チェック + コミット**

```bash
cd /home/kkouta/poc/zeimee
node --check script.js
git add script.js
git commit -m "feat(spec 11): poll vouchers every 5s while OCR is in-flight"
```

---

## Task 9: styles.css — OCR ステータス表示

**Files:** `styles.css`

- [ ] **Step 1: 末尾に追加**

```css
/* === Spec 11: OCR ステータス === */
.voucher-ocr {
  padding: 6px 8px;
  font-size: 11px;
  border-top: 1px solid #f3f4f6;
  line-height: 1.4;
}

.voucher-ocr.ocr-running {
  color: #6b7280;
  display: flex;
  align-items: center;
  gap: 4px;
}

.voucher-ocr.ocr-failed {
  color: #b91c1c;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.voucher-ocr-retry {
  background: #b91c1c;
  color: #fff;
  border: 0;
  border-radius: 4px;
  padding: 2px 8px;
  cursor: pointer;
  font-size: 10px;
}

.voucher-ocr.ocr-done .voucher-ocr-amount {
  font-weight: 700;
  color: #111827;
}

.voucher-ocr.ocr-done .voucher-ocr-vendor,
.voucher-ocr.ocr-done .voucher-ocr-date {
  color: #4b5563;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.spinner-sm {
  display: inline-block;
  width: 10px;
  height: 10px;
  border: 2px solid #e5e7eb;
  border-top-color: #6366f1;
  border-radius: 50%;
  animation: voucher-spin 0.8s linear infinite;
}
```

- [ ] **Step 2: コミット**

```bash
cd /home/kkouta/poc/zeimee
git add styles.css
git commit -m "feat(spec 11): styles for OCR status badges and retry button"
```

---

## Task 10: 手動 UI 検証 (人間)

- [ ] `.env` に `OPENAI_API_KEY` を設定 → サーバを再起動
- [ ] 領収書画像をアップロード → カードに「OCR 中…」スピナー
- [ ] 数秒後（or 自動ポーリング）に金額・発行者・日付がカードに出る
- [ ] OPENAI_API_KEY を一時的に invalid な値にして再起動 → アップロード → カードが「OCR 失敗 [再試行]」
- [ ] 「再試行」ボタンを押すと再度走る
- [ ] OPENAI_API_KEY を空にしてアップロード → ステータスは pending のまま（ポーリングしても変わらない）

---

## Task 11: 全体回帰

- [ ] **Step 1: vitest**

```bash
cd /home/kkouta/poc/zeimee/server && npx vitest run
```

Expected: 既存 + ocr-service 3 + voucher-service +4 + routes +4 全部 PASS。

- [ ] **Step 2: tsc**

```bash
cd /home/kkouta/poc/zeimee/server && npx tsc --noEmit 2>&1 | grep -v "loggerInstance\|Http2\|routes/mode" | head -20
```

Expected: spec 11 由来の新規エラー無し（既存の loggerInstance / Http2 / mode.ts は無視）。
